// test file for goal cli

// todo: doesn't work: #1152
// echo {args}

for i, fn := range goalib.SplitLines(`/bin/ls -1`) {
	fmt.Println(i, fn)
}

$ x := 1
$ y := 4
$ a := x * 2
$ b := x + y
$ c := x * y + a * b

fmt.Println(c)


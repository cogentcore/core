// test makefile for cosh

command build {
	println("running the build command")
}

command test {
	println("running the test command")
}

// if only "cosh, filename" args, then run default
// todo: make a more semantic function for this
// in the `make` package with other such helpful functions
if len(os.Args) == 2 {
	build
}

